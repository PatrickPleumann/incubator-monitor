package io.github.patrickpleumann.incubator;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/**
 * Proves that the test infrastructure runs at all. Removed once real tests exist.
 */
class BuildSetupTest {

    @Test
    void testInfrastructureRuns() {
        assertEquals(2, 1 + 1);
    }
}
